# v0.1.0 Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete Markdown support, progress feedback, model config cleanup, branding, remote setup, and v0.1.0 release tagging.

**Architecture:** Extend the existing FastAPI + SQLite backend with task progress fields and Markdown parsing. Update the React workbench forms and progress panels without changing the approved layout. Use the provided PNG as the single source logo/icon asset for web and Tauri.

**Tech Stack:** Python FastAPI, SQLite, React TypeScript, Vite, Tauri v2, PNG static assets, Git.

---

### Task 1: Branch and Planning Baseline

- [ ] Merge `implement-local-runnable` into `main`.
- [ ] Save this design and implementation plan under `docs/superpowers`.
- [ ] Commit the planning baseline.

### Task 2: Markdown and Backend Progress

- [ ] Add `.md` and `.markdown` to upload support.
- [ ] Add Markdown parser and route Markdown files through parse service.
- [ ] Add task progress columns to `task_logs` with safe migrations.
- [ ] Update log writer to accept progress values.
- [ ] Add task summary endpoint for frontend polling.
- [ ] Emit progress during parse, chunk, vector index, and checklist generation.
- [ ] Extend backend test to upload Markdown and assert progress fields exist.

### Task 3: Frontend Progress UI and Model Config

- [ ] Update frontend file accept list to include Markdown.
- [ ] Add progress bar component behavior through `TaskLogPanel`.
- [ ] Show running state in index build and checklist generation actions.
- [ ] Split model config form behavior by `chat` and `embedding`.
- [ ] Remove vector dimension from chat model form.
- [ ] Use select options for embedding dimensions.

### Task 4: Branding Assets

- [ ] Copy user image to frontend public logo and favicon.
- [ ] Replace sidebar logo display.
- [ ] Update HTML favicon.
- [ ] Replace Tauri icon PNG.
- [ ] Verify frontend build and Tauri build.

### Task 5: Verification and Release

- [ ] Run backend tests.
- [ ] Run frontend build.
- [ ] Run Tauri cargo check.
- [ ] Run Tauri app build.
- [ ] Commit changes on `main`.
- [ ] Add GitHub remote.
- [ ] Push `main`.
- [ ] Create and push `v0.1.0`.

